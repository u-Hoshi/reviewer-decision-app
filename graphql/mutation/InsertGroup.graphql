mutation InsertGroup($slackUrl: String!, $responsible_id: Int!, $name: String!) {
  insert_group_one(object: { name: $name, responsible_id: $responsible_id, slackUrl: $slackUrl }) {
    id
    name
    responsible_id
    slackUrl
  }
}
